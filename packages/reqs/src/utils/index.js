import { requestBatch } from './request-batch.js';
import { requestFallbackProvider } from './request-fallback-provider.js';

/**
 * Namespace containing utils for working with requests.
 * @module Utils
 */
export {
  requestBatch,
  requestFallbackProvider,
};