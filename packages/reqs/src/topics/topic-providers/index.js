import { provideTopics } from "./provide-topics.js";

/**
 * Namespace containing APIs for messaging between a requesting app and a Topic Provider.
 * @module TopicProviders
 */
export {
  provideTopics,
};
